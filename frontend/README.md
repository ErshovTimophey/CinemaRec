# CinemaRec Frontend (Auth Service)

This is the frontend part of the CinemaRec Auth Service, built with React.

## Installation

1. Clone the repository.
2. Install dependencies:
   ```bash
   npm install