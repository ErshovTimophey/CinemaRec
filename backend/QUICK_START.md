# Быстрый запуск проекта CinemaRec

## Самый простой способ (Windows)

1. **Откройте командную строку (CMD) или PowerShell**

2. **Перейдите в папку backend:**
   ```cmd
   cd backend
   ```

3. **Запустите автоматический скрипт:**
   ```cmd
   docker-compose-up.bat
   ```

   Этот скрипт автоматически:
   - ✅ Соберёт все JAR файлы для всех сервисов
   - ✅ Запустит Docker Compose
   - ✅ Запустит все сервисы в фоновом режиме

4. **Дождитесь завершения** (первый запуск может занять 5-10 минут)

5. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

## Проверка статуса

```cmd
# Проверить статус всех контейнеров
docker-compose ps

# Посмотреть логи сервиса
docker-compose logs -f statistics-service
docker-compose logs -f user-service
docker-compose logs -f frontend
```

## Остановка сервисов

```cmd
docker-compose down
```

## Если что-то пошло не так

### Ошибка JAVA_HOME:
```cmd
set-java-home.bat
docker-compose-up.bat
```

### Пересборка с нуля:
```cmd
docker-compose down
docker-compose up --build -d
```

### Просмотр всех логов:
```cmd
docker-compose logs
```

## Порты сервисов

- **Frontend**: http://localhost:3000
- **Statistics Service**: http://localhost:8088
- **User Service**: http://localhost:8082
- **Auth Service**: http://localhost:8081

## Тестирование просмотра трейлеров

1. Войдите в систему (зарегистрируйтесь или войдите)
2. Перейдите в "Recommendations" или "Statistics"
3. Нажмите кнопку **"Смотреть"** на любой карточке фильма
4. Наслаждайтесь просмотром трейлеров! 🎬
